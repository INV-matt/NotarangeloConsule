const Formula = (title, src) => {
  <>
    <img src={src} alt={title} />
    <p>{title}</p>
  </>
}


const Formulas = () => {
  return ( <div className="formulas">
    0
  </div> );
}
 
export default Formulas;